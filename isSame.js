let arr1 = [3, 5, 4, 6, 1, 0, 2]; // i
let arr2 = [3, 1, 5, 2, 4, 6, 0]; // j


const isSame = (arr1, arr2) => {
    //base cases
    if(arr1.length !== arr2.length || arr1.length === 0 || arr1[0] !== arr2[0]) {
        return false;
    }

    let arr1Left = [];
    let arr1Right = [];
    let arr2Left = [];
    let arr2Right =[];

    for(let i=1; i<arr1.length; i++)
    

};